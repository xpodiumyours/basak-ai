# Basak — calisma ortami bagimliliklari
# Surumler bu bilgisayarda OLCULDU (2026-09-19, Python 3.12.10).
# Kurulum:
#     python -m pip install -r requirements.txt
#
# Not: bu dosya bugune kadar YOKTU; baska bir makinede kurulum
# imkansizdi. Yeni bagimlilik eklendiginde buraya da yazilir.

# ── Cekirdek: beyin (bulut saglayici istemcileri) ──────────────────
openai==2.20.0
requests==2.32.5
cohere==7.0.9

# ── Web arama ve sayfa okuma ───────────────────────────────────────
ddgs==9.14.4
fastapi==0.117.1

# ── Masaustu arayuz ────────────────────────────────────────────────
pywebview==6.2.1; sys_platform == "win32"
pystray==0.19.5; sys_platform == "win32"
Pillow==12.1.1; sys_platform == "win32"

# ── Kalici hafiza (vektor arama; sqlite-vec yoksa BM25'e duser) ─────
# sqlite-vec: manylinux wheel'i var; Linux'ta da anlam aramasi acik kalsin
# (yoksa motor yine BM25'e duser — dayanikli, build riski sifir).
sqlite-vec==0.1.9
# pyannote.audio 4.0.7 -> pyannote-core 6.0.1 -> numpy>=2.0.
# 1.26.4 pini pip cozumunu kiriyordu; Python 3.12 icin guncel kararlı
# NumPy 2.5.3 sabitlenir ki CI ve masaustu ayni bagimlilik setini kullansin.
# 2026-09-22: kosul GERI ALINDI — memory/ motoru Vercel (Linux)da da
# numpy ister; "yalniz win32" isareti bulutta hafizayi sessizce devre
# disi birakiyordu (log: "Hafiza motoru acilamadi: No module numpy").
# pyannote zaten win32 kosullu, Linux'a inmez.
numpy==2.5.3

# ── Ses: TTS (metin->ses) + STT (ses->metin) + konusmaci tanima ────
# DIKKAT: piper-tts paketi ses MODELINI icermez. tr_TR-dfki-medium.onnx
# ayri indirilir — README "Ses modeli" bolumune bak.
piper-tts==1.7.0; sys_platform == "win32"
sounddevice==0.5.5; sys_platform == "win32"
soundfile==0.13.1; sys_platform == "win32"
faster-whisper==1.2.1; sys_platform == "win32"
pyannote.audio==4.0.7; sys_platform == "win32"

# ── Telegram koprusu ───────────────────────────────────────────────
python-telegram-bot==22.8; sys_platform == "win32"

# ── Test ───────────────────────────────────────────────────────────
pytest==9.1.1; sys_platform == "win32"
