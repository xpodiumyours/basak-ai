const chatEl=document.getElementById("chat");const msgEl=document.getElementById("message");const sendEl=document.getElementById("send");const history=[];function bubble(role,text,meta){const el=document.createElement("div");el.className="bubble "+role;el.textContent=text;if(meta){const m=document.createElement("span");m.className="meta";m.textContent=meta;el.appendChild(m)}chatEl.appendChild(el);chatEl.scrollTop=chatEl.scrollHeight;return el}async function send(){const text=msgEl.value.trim();if(!text||sendEl.disabled)return;msgEl.value="";history.push({role:"user",content:text});bubble("user",text);sendEl.disabled=true;const wait=bubble("assistant","Başak düşünüyor…","ücretsiz sağlayıcı zinciri");try{const r=await fetch("/api/chat",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({messages:history})});const d=await r.json();wait.remove();if(!r.ok||!d.ok)throw new Error(d.error||"Sohbet isteği başarısız");history.push({role:"assistant",content:d.text});bubble("assistant",d.text,(d.provider||"?")+" · "+(d.model||"?"))}catch(e){wait.remove();bubble("assistant","Hata: "+(e.message||e),"Bu mesaj kabul kanıtı sayılmaz")}finally{sendEl.disabled=false;msgEl.focus()}}sendEl.addEventListener("click",send);msgEl.addEventListener("keydown",e=>{if(e.key==="Enter"&&!e.shiftKey){e.preventDefault();send()}});
