async function basakHealth(){try{const r=await fetch("/api/health",{cache:"no-store"});const d=await r.json();const dot=document.getElementById("healthDot"),text=document.getElementById("healthText");if(dot)dot.classList.toggle("ok",Boolean(d.ok));if(text)text.textContent=d.ok?"Worker hazır":"Worker hatası";return d}catch(e){const text=document.getElementById("healthText");if(text)text.textContent="Bağlantı yok";return null}}window.basakHealth=basakHealth;basakHealth();
